import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
/*
 * Eliminate dall'array i numeri dispari, modificate ogni valore moltiplicando se stesso per 2 e stampate il
 * risultato. Utilizzate le lambda expression ed i reference method dove possibile.
 * forEach(Consumer<? super T> action)
 * removeIf(Predicate<? super E> filter)
 * replaceAll(UnaryOperator<E> operator)
 */
public class Main {
	public static void main(String[] args) {
		List<Integer> integerList = new ArrayList<>(Arrays.asList(2, 5, 8, 10, 20, 15, 35, 15, 8, 12, 3));
	}
}
